name: FT_PRINTF_TEST

on:
  push:
    branches: [main, libft]

jobs:
  build:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2
      - name: make
        run: make run
        working-directory: ./ft_printf/FT_PRINTF_TEST
